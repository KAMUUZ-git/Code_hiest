# ============================================================
#  CAMPUS DIGITAL TWIN v2 — Dependency Manifest
#  Bounded RANGES (not hard pins) so pip resolves prebuilt
#  wheels for any runtime Python: 3.9 (local Mac) through
#  3.12 / 3.13 / 3.14 (Render). Hard pins broke the Render
#  build on Python 3.14 (no cp314 wheels for the old pins).
# ============================================================

# ── Backend Engine ──────────────────────────────────────────
fastapi>=0.111,<1              # REST + native WebSocket support
uvicorn[standard]>=0.29,<1     # ASGI server; [standard] = websockets + httptools

# ── Persistence (SQLite default / Supabase Postgres optional) ──
sqlmodel>=0.0.16,<0.1          # SQLAlchemy 2.0 + Pydantic v2 hybrid ORM
sqlalchemy>=2.0.30,<2.1        # explicit floor for reproducible behavior
psycopg2-binary>=2.9.9,<3      # only used when DATABASE_URL points at Postgres

# ── Validation layer ────────────────────────────────────────
pydantic>=2.7.1,<3             # request/response schemas

# ── Simulator (HTTP fallback client) ────────────────────────
requests>=2.32.3,<3
